package grupo04.truetestu.model.enums;

public enum EstadoEstudiante {
    /** Estudiante actualmente matriculado. */
    ACTIVO,
    /** Estudiante no matriculado, pero puede reactivarse. */
    INACTIVO,
    /** Estudiante suspendido temporalmente. */
    SUSPENDIDO,
    /** Estudiante que ha completado sus estudios. */
    GRADUADO
}
